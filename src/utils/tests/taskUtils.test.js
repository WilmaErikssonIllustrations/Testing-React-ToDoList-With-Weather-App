import { describe, expect, test } from "vitest";
import {
  validateTitle,
  filterTasks,
  countActiveTasks,
  countCompletedTasks,
} from "../taskUtils";

describe("taskUtils", () => {
  describe("validateTitle", () => {
    test("error if title is empty");
    //arrange
    //act
    //assert
  });
});
